# V1 TLS 715 Recovery Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add explicit A7670G HTTPS configuration and bounded recovery from status 715.

**Architecture:** Configure SSL context 0 inside the existing HTTP transaction. Track 715 across the existing three-attempt round and request one PB8 cycle in the outer 60-second retry path.

**Tech Stack:** STM32L452 C firmware, A7670G AT commands, PowerShell source-contract tests, GNU Arm Embedded build

## Global Constraints

- Preserve the CSV payload and SD queue.
- Preserve three HTTP attempts, five-second attempt backoff, and 60-second queue backoff.
- Do not power-cycle for non-715 upload failures.
- Do not modify unrelated CubeIDE-generated files.

---

### Task 1: Add TLS 715 recovery

**Files:**
- Create: `tests/v1_tls_715_recovery_test.ps1`
- Modify: `Core/Src/main.c`

**Interfaces:**
- Consumes: `HTTP_PostFile()`, `Upload_OldestReady()`, `Drain_UploadQueueBeforeNextRecord()`
- Produces: explicit SSL context configuration and one pending recovery cycle after a failed 715 round

- [ ] **Step 1: Write and run the failing source-contract test**

Require TLS 1.2, SNI, HTTP SSL-context selection, Live Expressions, round-level 715 tracking, and a TLS-only PB8 recovery path.

- [ ] **Step 2: Implement the minimal firmware change**

Add the three AT commands, counters, round-local 715 observation, pending flag, and recovery branch.

- [ ] **Step 3: Verify**

Run the new test, all `tests/*.ps1`, and `make -C Debug all -j2` with CubeIDE's bundled compiler on `PATH`.

- [ ] **Step 4: Commit**

Commit only the source, new test, design, and plan.
